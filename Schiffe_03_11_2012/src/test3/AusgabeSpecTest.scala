package test3
import org.scalatest._
import org.specs2.mutable._

import schiffe.Ausgabe;


class AusgabeSpecTest extends FlatSpec{
  "A new Ausgabe" should {
    val ausgabe = new Ausgabe(1)
       

//    "generate a String of the form (2, 3) = 0" in {
//      cell.mkString must be_==("(2, 3) = 0")
//    }

  }
}